{
  "builds": [
    { "src": "_backend/backend/wsgi.py", "use": "@vercel/python" }
  ],
  "buildCommand": "cd _frontend/app && npm ci && npm run build && cd ../../_backend && pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate --noinput",
  "routes": [
    { "src": "/(.*)", "dest": "_backend/backend/wsgi.py" }
  ],
  "env": {
    "PYTHONPATH": "_backend"
  }
}
